/////////////////////////////////////////////////////////////////////////////////
// reachable rule, fails on bugs always reverting (instead of being unreachable)
/////////////////////////////////////////////////////////////////////////////////
rule revertsNotAllways(method f, env e, calldataarg arg){
    f(e, arg);

    satisfy true;
}
