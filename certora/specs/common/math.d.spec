/////////////////////////////////////////////////////////////////////////////////
// simple min and max definitions on mathint
/////////////////////////////////////////////////////////////////////////////////
definition min(mathint a, mathint b) returns mathint = ( a <= b ? a : b );
definition max(mathint a, mathint b) returns mathint = ( a >= b ? a : b );
